### A strict Webpack-TypeScript starter
---

Requires Node 11.x (or add a polyfill for `flatMap` inside build/webpack.config.dev.js).

TODO: Add better readme describing features/info about configuration.
